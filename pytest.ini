[pytest]
asyncio_mode = auto
markers =
    integration: marks integration tests that may touch multiple layers
    slow: marks slower tests
    discord_stub: tests using stubbed discord objects
    live_crcon: tests hitting a live CRCON instance (opt-in via env)
